# Csharf

כלי טוויקים ותחזוקה ל-Windows, בעברית. בלי התקנה.

שתי מהדורות, לפי מה שמתאים לך:

| | **[2.0 — ממשק גרפי](../../releases/tag/v2.0)** | **[1.0 — קונסולה](../../releases/tag/v1.0)** |
|---|---|---|
| קובץ | `Csharf.bat` | `Csharf-CMD.bat` |
| פעולות | 18 | 9 |
| מה זה מריץ | batch + PowerShell | batch בלבד |
| חלון | ממשק גרפי | חלון קונסולה קלאסי |

שתיהן קובץ אחד, לחיצה כפולה, ומבקשות הרשאות מנהל בעצמן.

---

## 2.0 — ממשק גרפי

**ביצועים וחשמל** · מצב ביצועים גבוהים · מצב אולטימטיבי, עם עקיפה למהדורות שבהן הסכימה מוסתרת · חזרה למאוזן · השבתה והפעלה של מצב תרדמה · דוח בריאות סוללה

**ניקוי ושטח** · פינוי שטח וניקוי מערכת · שחרור האחסון השמור של Windows · תיקון קבצי מערכת ב-DISM ו-SFC

**גישה מהירה וממשק** · קיצור לרשימת כל התוכנות · קיצור ל-God Mode · החזרת תפריט ההקשר הישן ב-Windows 11 · הצגת סיומות וקבצים מוסתרים

**רשת** · איפוס ערימת הרשת · הצגת סיסמאות ה-Wi-Fi השמורות

**מערכת ורישוי** · יצירת נקודת שחזור · הצגת מפתח המוצר מהקושחה · שדרוג מהדורה מ-Home ל-Pro

אחרי כל פעולה חוזר לתפריט ומציג ביומן מה בדיוק קרה. פעולות ארוכות רצות בלי להקפיא את החלון.

## 1.0 — קונסולה

תפריט קלאסי בחלון אחד, **בלי PowerShell בכלל**: ביצועים גבוהים · אולטימטיבי · מאוזן · פינוי שטח · השבתה והפעלה של תרדמה · קיצור לתוכנות · נקודת שחזור · שדרוג ל-Pro.

הגבהת הרשאות ונקודת השחזור עוברות דרך VBS ו-`cscript`, כי `wmic` הוסר מגרסאות Windows חדשות.

## ⚠ על שינוי המהדורה ל-Pro

שתי המהדורות משתמשות ב**מפתח הגנרי הפומבי של מיקרוסופט**.
הפעולה **משנה את המהדורה בלבד — היא לא מפעילה רישיון (Activation).**
אחרי ההפעלה מחדש עדיין נדרש רישיון Pro תקף או זכאות דיגיטלית.

## תאימות

**Windows 10/11, כל המהדורות.** 2.0 דורש PowerShell 3.0 ומעלה, מובנה מ-Windows 8. 1.0 לא דורש כלום מעבר ל-cmd.

חלק מהפעולות קיימות רק מגרסה מסוימת — תפריט ההקשר הוא Windows 11 בלבד, אחסון שמור מ-1903, שינוי מהדורה מ-1511, ונקודת שחזור לא קיימת ב-Windows Server. פעולה שאינה נתמכת מדולגת עם הודעה, בלי לקרוס.

## למה זה בנוי ככה

**2.0** הוא batch ו-PowerShell באותו קובץ. ה-batch מחלץ את קטע ה-PowerShell, מריץ אותו מוסתר ויוצא מיד; ה-PowerShell מבקש הרשאות פעם אחת ומציג WinForms. הסיבה היא ש-WinForms תומך ב-`RightToLeft` אמיתי, כך שהעברית נכתבת בסדר לוגי רגיל ונקראת נכון.

**1.0** לא יכול להישען על זה, כי לקונסולה אין bidi — היא מציירת תווים משמאל לימין לפי סדר האחסון. לכן העברית שם שמורה ב**סדר ויזואלי**, והקובץ שמור ב**קוד דף 862** (עברית OEM).

הקידוד הוא לא רק עניין של גופן: cmd עוקב אחרי היסט בייטים בקובץ ה-bat בזמן שהוא מפענח אותו, ו-UTF-8 רב-בייטי מוציא אותו מסנכרון עד שהוא מתחיל להריץ אמצע שורות כפקודות. ב-CP862 כל אות עברית היא בייט אחד, וההיסט נשאר תקין.

היפוך ידני של טקסט הוא מה ששיבש גרסאות קודמות של הכלי, ולכן `tools/build-cmd.ps1` כתוב בעברית לוגית רגילה וממיר לסדר ויזואלי מכנית. **לערוך את הגנרטור, לא את ה-bat.**

> **לעורכים את `Csharf.bat`:** החלק העליון, עד שורת הסימון שלפני קטע ה-PowerShell,
> חייב להישאר ASCII בלבד — מאותה סיבה בדיוק. הקובץ גם חייב להישמר כ-UTF-8
> **בלי BOM**, אחרת `@echo off` עצמו נשבר. `.gitattributes` מסמן `*.bat -text`
> כדי ש-git לא ינרמל שורות ויפגע בשני הקבצים.

## רישיון

MIT
